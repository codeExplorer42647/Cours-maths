{
  "name": "cours-maths",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify": "terser p1984.js -o p1984.min.js --compress --mangle",
    "watch": "terser p1984.js -o p1984.min.js --compress --mangle --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "terser": "^5.30.0"
  }
}
